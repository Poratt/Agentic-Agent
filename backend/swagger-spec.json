{
  "openapi": "3.0.0",
  "paths": {
    "/auth/register": {
      "post": {
        "description": "Creates a new user in the database using the provided credentials. Required fields (from RegisterDto): email (string, valid email format), password (string, min 6 chars), username? (string, optional). Returns the newly created User entity (without password). Returns 400 if email is already taken or DTO validation fails. This endpoint is public — no token required.",
        "operationId": "AuthController_register",
        "parameters": [],
        "requestBody": {
          "required": true,
          "description": "Registration payload. Example: { \"email\": \"user@example.com\", \"password\": \"secret123\", \"username\": \"myName\" }.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterDto"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "User created successfully. Returns the User entity: id (number), email (string), username (string), role (\"user\" | \"admin\"), createdAt (ISO date). Password is never returned.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/User"
                }
              }
            }
          },
          "400": {
            "description": "DTO validation failed (missing fields, invalid email format, weak password) or email is already registered."
          },
          "401": {
            "description": "Not applicable for this endpoint."
          },
          "403": {
            "description": "Not applicable for this endpoint."
          },
          "404": {
            "description": "Not applicable for this endpoint."
          },
          "500": {
            "description": "Unexpected server error."
          }
        },
        "summary": "Register a new user account",
        "tags": [
          "auth"
        ]
      }
    },
    "/auth/login": {
      "post": {
        "description": "Validates the provided credentials against the database. On success: sets the access token and refresh token as HTTP-only cookies (or Authorization header — depending on AuthService configuration) and returns the User entity. Required fields (from LoginDto): email (string), password (string). Returns 401 if email does not exist or password does not match. This endpoint is public — no token required.",
        "operationId": "AuthController_login",
        "parameters": [],
        "requestBody": {
          "required": true,
          "description": "Login credentials. Example: { \"email\": \"user@example.com\", \"password\": \"secret123\" }.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LoginDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Login successful. Returns the User entity (id, email, username, role, createdAt). Auth tokens are set as cookies/headers by the service.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/User"
                }
              }
            }
          },
          "400": {
            "description": "DTO validation failed (missing or malformed fields)."
          },
          "401": {
            "description": "Invalid credentials — email not found or password does not match."
          },
          "403": {
            "description": "Not applicable for this endpoint."
          },
          "404": {
            "description": "Not applicable for this endpoint."
          },
          "500": {
            "description": "Unexpected server error."
          }
        },
        "summary": "Login with email and password",
        "tags": [
          "auth"
        ]
      }
    },
    "/auth/refresh": {
      "post": {
        "description": "JwtRefreshGuard reads the refresh token from the cookie or Authorization header. If valid, AuthService issues a new access token and writes it to cookies/headers. req.user is populated by the guard with: sub (userId), refreshToken (raw string). Returns 401 if the refresh token is absent, expired, or does not match the stored hash. Call this endpoint when the access token has expired (typically 401 on any protected route).",
        "operationId": "AuthController_refresh",
        "parameters": [],
        "responses": {
          "200": {
            "description": "New access token issued. Returns the User entity. Updated tokens are set in cookies/headers by the service.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/User"
                }
              }
            }
          },
          "400": {
            "description": "Malformed request."
          },
          "401": {
            "description": "Refresh token is missing, expired, or does not match the stored token hash."
          },
          "403": {
            "description": "Not applicable for this endpoint."
          },
          "404": {
            "description": "Not applicable for this endpoint."
          },
          "500": {
            "description": "Unexpected server error."
          }
        },
        "summary": "Refresh access token using refresh token",
        "tags": [
          "auth"
        ]
      }
    },
    "/auth/logout": {
      "post": {
        "description": "Reads the authenticated user from req.user (populated by JwtAuthGuard). Calls AuthService.logout(userId, res) which: (1) clears the refresh token hash from the database, (2) clears auth cookies/headers from the response. After this call, the refresh token is revoked and the access token will be rejected on the next request (or expire naturally). Requires Authorization: Bearer <accessToken> header.",
        "operationId": "AuthController_logout",
        "parameters": [],
        "responses": {
          "200": {
            "description": "Logout successful. Session cleared. Returns ServiceResultContainer with success=true."
          },
          "400": {
            "description": "Malformed request."
          },
          "401": {
            "description": "Access token is missing, expired, or invalid."
          },
          "403": {
            "description": "Not applicable for this endpoint."
          },
          "404": {
            "description": "Not applicable for this endpoint."
          },
          "500": {
            "description": "Unexpected server error."
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Logout and invalidate session [JWT required]",
        "tags": [
          "auth"
        ]
      }
    },
    "/auth/me": {
      "get": {
        "description": "Reads req.user as populated by JwtAuthGuard (decoded from the access token). No database query is made. Returns a ServiceResultContainer<JwtPayload> where result contains: sub (userId number), email (string), role (\"user\" | \"admin\"), iat (issued-at timestamp), exp (expiry timestamp). Use this to check who is logged in or to verify the token is still valid. Requires Authorization: Bearer <accessToken> header.",
        "operationId": "AuthController_me",
        "parameters": [],
        "responses": {
          "200": {
            "description": "ServiceResultContainer with success=true and result = JwtPayload (sub, email, role, iat, exp). No database round-trip.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/User"
                }
              }
            }
          },
          "400": {
            "description": "Malformed request."
          },
          "401": {
            "description": "Access token is missing, expired, or invalid."
          },
          "403": {
            "description": "Not applicable for this endpoint."
          },
          "404": {
            "description": "Not applicable for this endpoint."
          },
          "500": {
            "description": "Unexpected server error."
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Get current authenticated user [JWT required]",
        "tags": [
          "auth"
        ]
      }
    },
    "/users": {
      "get": {
        "description": "Returns every user in the system with safe/public fields only (id, username, email, role, createdAt). Passwords and sensitive tokens are never included. Use this endpoint to discover user IDs before calling GET /users/:id.",
        "operationId": "UsersController_list",
        "parameters": [],
        "responses": {
          "200": {
            "description": "Array of users (safe fields). Each item contains: id (number), username (string), email (string), role (string: \"admin\" | \"user\"), createdAt (ISO date string)."
          },
          "401": {
            "description": "No valid JWT Bearer token was provided. Add the Authorization header: \"Bearer <token>\"."
          },
          "500": {
            "description": "Unexpected server error."
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "List all users",
        "tags": [
          "users"
        ]
      }
    },
    "/users/me": {
      "get": {
        "description": "Reads the JWT payload that was decoded by JwtAuthGuard and returns it as a ServiceResultContainer<JwtPayload>. Fields: sub (userId number), email (string), role (string), iat (number), exp (number). No database query is made — data comes directly from the token.",
        "operationId": "UsersController_me",
        "parameters": [],
        "responses": {
          "200": {
            "description": "ServiceResultContainer with success=true and result = JwtPayload object (sub, email, role, iat, exp)."
          },
          "401": {
            "description": "Missing or expired JWT token."
          },
          "500": {
            "description": "Unexpected server error."
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Get current authenticated user",
        "tags": [
          "users"
        ]
      }
    },
    "/users/{id}": {
      "get": {
        "description": "Fetches a single user record by numeric id with safe/public fields only. Throws 404 if the user does not exist. Use GET /users first to discover valid IDs.",
        "operationId": "UsersController_getById",
        "parameters": [
          {
            "name": "id",
            "required": true,
            "in": "path",
            "description": "The unique numeric database id of the user (e.g. 1, 42). Must be a positive integer — ParseIntPipe will reject anything else.",
            "schema": {
              "type": "number"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Single user object (safe fields): id, username, email, role, createdAt."
          },
          "401": {
            "description": "Missing or expired JWT token."
          },
          "404": {
            "description": "No user found with the given id."
          },
          "500": {
            "description": "Unexpected server error."
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Get user by id",
        "tags": [
          "users"
        ]
      },
      "patch": {
        "description": "Updates one or more fields of the user identified by :id. Send only the fields you want to change (partial update). Available fields in UpdateUserDto: username? (string), email? (string). Role changes must use PATCH /users/:id/role instead. Requires admin privileges.",
        "operationId": "UsersController_update",
        "parameters": [
          {
            "name": "id",
            "required": true,
            "in": "path",
            "description": "Numeric id of the user to update.",
            "schema": {
              "type": "number"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "description": "Partial user data. All fields are optional. Example: { \"username\": \"newName\" } or { \"email\": \"new@example.com\" }.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateUserDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Updated user record (safe fields)."
          },
          "401": {
            "description": "Missing or expired JWT token."
          },
          "403": {
            "description": "Authenticated user does not have the admin role. AdminGuard rejected the request."
          },
          "404": {
            "description": "No user found with the given id."
          },
          "500": {
            "description": "Unexpected server error."
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Update user details [Admin]",
        "tags": [
          "users"
        ]
      },
      "delete": {
        "description": "Hard-deletes the user record from the database. IRREVERSIBLE — no soft-delete or recycle bin. Double-check the id before calling this endpoint. Requires admin privileges.",
        "operationId": "UsersController_delete",
        "parameters": [
          {
            "name": "id",
            "required": true,
            "in": "path",
            "description": "Numeric id of the user to delete.",
            "schema": {
              "type": "number"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Deletion confirmed. Returns ServiceResultContainer with success=true."
          },
          "401": {
            "description": "Missing or expired JWT token."
          },
          "403": {
            "description": "Authenticated user is not an admin."
          },
          "404": {
            "description": "No user found with the given id."
          },
          "500": {
            "description": "Unexpected server error."
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Delete user permanently [Admin]",
        "tags": [
          "users"
        ]
      }
    },
    "/users/{id}/role": {
      "patch": {
        "description": "Sets the role field of the user identified by :id. Valid roles (from UpdateUserRoleDto): \"admin\" | \"user\". Use this endpoint exclusively for role changes; other user fields are updated via PATCH /users/:id. Requires admin privileges.",
        "operationId": "UsersController_updateRole",
        "parameters": [
          {
            "name": "id",
            "required": true,
            "in": "path",
            "description": "Numeric id of the user whose role will change.",
            "schema": {
              "type": "number"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "description": "Role payload. Required field: role (string enum: \"admin\" | \"user\"). Example: { \"role\": \"admin\" }.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateUserRoleDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Role updated successfully. Returns ServiceResultContainer with updated user."
          },
          "401": {
            "description": "Missing or expired JWT token."
          },
          "403": {
            "description": "Authenticated user is not an admin."
          },
          "404": {
            "description": "No user found with the given id."
          },
          "500": {
            "description": "Unexpected server error."
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Change user role [Admin]",
        "tags": [
          "users"
        ]
      }
    },
    "/admin-agent/query-stream": {
      "post": {
        "description": "Streams the model output back to the client using Server-Sent Events (SSE). Recommended for long generations.",
        "operationId": "AdminAgentController_streamChat",
        "parameters": [],
        "requestBody": {
          "required": true,
          "content": {
            "multipart/form-data": {
              "schema": {
                "$ref": "#/components/schemas/AgentRequestDto"
              }
            },
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AgentRequestDto"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "SSE stream opened; response body is streamed incrementally"
          },
          "401": {
            "description": "Missing/invalid access token"
          }
        },
        "security": [
          {
            "bearer": []
          }
        ],
        "summary": "Query Admin agent (stream)",
        "tags": [
          "Admin Agent"
        ]
      }
    }
  },
  "info": {
    "title": "API",
    "description": "",
    "version": "1.0",
    "contact": {}
  },
  "tags": [],
  "servers": [],
  "components": {
    "schemas": {
      "RegisterDto": {
        "type": "object",
        "properties": {
          "fullName": {
            "type": "string",
            "example": "John Doe",
            "description": "Full display name for the new account. Required - must be a non-empty string. Validation: returns 400 if missing, empty, or whitespace-only. Uniqueness: not required to be unique."
          },
          "email": {
            "type": "string",
            "example": "admin@admin.com",
            "format": "email",
            "description": "Email address for the new account. Required - must be a valid email format and must be unique. Validation: returns 400 if the email format is invalid. Uniqueness: returns 400 if already registered."
          },
          "password": {
            "type": "string",
            "example": "admin",
            "minLength": 4,
            "description": "Password for the new account. Minimum 4 characters. Required - stored as a bcrypt hash (never stored as plain text). Validation: returns 400 if shorter than 4 characters."
          }
        },
        "required": [
          "fullName",
          "email",
          "password"
        ]
      },
      "User": {
        "type": "object",
        "properties": {
          "id": {
            "type": "number",
            "description": "Unique identifier of the user",
            "example": 1
          },
          "email": {
            "type": "string",
            "description": "User email address",
            "example": "user@example.com"
          },
          "fullName": {
            "type": "string",
            "description": "Full name of the user",
            "example": "John Doe"
          },
          "password": {
            "type": "string",
            "description": "Hashed user password",
            "example": "hashed_password_123",
            "writeOnly": true
          },
          "refreshToken": {
            "type": "string",
            "description": "JWT refresh token for session management",
            "example": "ref_token_abc123",
            "nullable": true
          },
          "role": {
            "type": "number",
            "description": "User role within the system",
            "enum": [
              1,
              2
            ],
            "example": 2
          },
          "createdAt": {
            "format": "date-time",
            "type": "string",
            "description": "Timestamp when the user was created",
            "example": "2026-05-12T10:00:00Z"
          },
          "updatedAt": {
            "format": "date-time",
            "type": "string",
            "description": "Timestamp when the user was last updated",
            "example": "2026-05-12T11:00:00Z"
          },
          "lastLoginAt": {
            "format": "date-time",
            "type": "string",
            "description": "Timestamp of the user's last successful login",
            "example": "2026-05-20T10:00:00Z",
            "nullable": true
          }
        },
        "required": [
          "id",
          "email",
          "fullName",
          "password",
          "refreshToken",
          "role",
          "createdAt",
          "updatedAt",
          "lastLoginAt"
        ]
      },
      "LoginDto": {
        "type": "object",
        "properties": {
          "email": {
            "type": "string",
            "example": "admin@admin.com",
            "format": "email",
            "description": "Registered email address of the user. Must be a valid email format (user@domain.com). Case-insensitive — \"Admin@Admin.com\" and \"admin@admin.com\" are treated as the same."
          },
          "password": {
            "type": "string",
            "example": "admin",
            "minLength": 4,
            "description": "Account password. Minimum 4 characters. Sent as plain text over HTTPS — hashing is handled server-side. Returns 401 if this does not match the stored hash."
          }
        },
        "required": [
          "email",
          "password"
        ]
      },
      "UserRole": {
        "type": "number",
        "enum": [
          1,
          2
        ],
        "description": "New role for the user. Optional — omit to keep the current role. Validation: must be a valid UserRole value; returns 400 if invalid. Accepted values: \"user\" | \"admin\". Prefer using PATCH /users/:id/role for role-only changes (more explicit and easier to audit)."
      },
      "UpdateUserDto": {
        "type": "object",
        "properties": {
          "fullName": {
            "type": "string",
            "example": "John Updated",
            "description": "Full display name of the user. Optional — omit this field to leave the current value unchanged. Validation: if provided, must be a string and must not be empty (whitespace-only is rejected). Returns 400 if the value is empty while provided."
          },
          "email": {
            "type": "string",
            "example": "updated@example.com",
            "format": "email",
            "description": "New email address for the user. Optional — omit to keep the current email. Validation: must be a valid email format if provided; returns 400 if invalid. Uniqueness: returns 400 if the email is already taken by another account."
          },
          "role": {
            "example": 2,
            "description": "New role for the user. Optional — omit to keep the current role. Validation: must be a valid UserRole value; returns 400 if invalid. Accepted values: \"user\" | \"admin\". Prefer using PATCH /users/:id/role for role-only changes (more explicit and easier to audit).",
            "allOf": [
              {
                "$ref": "#/components/schemas/UserRole"
              }
            ]
          }
        }
      },
      "UpdateUserRoleDto": {
        "type": "object",
        "properties": {
          "role": {
            "example": 2,
            "description": "The new role to assign to the user. Required — this DTO represents a role-only update. Accepted values: \"user\" (standard access) | \"admin\" (full system access). Validation: returns 400 if the value is not a valid UserRole enum member. Security note: use \"admin\" with caution — it grants access to admin-only endpoints.",
            "allOf": [
              {
                "$ref": "#/components/schemas/UserRole"
              }
            ]
          }
        },
        "required": [
          "role"
        ]
      },
      "AgentRequestDto": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "description": "The user prompt that will be sent to the AI agent. This should include enough context and the desired output format.",
            "example": "Write a short summary of the following text..."
          }
        },
        "required": [
          "prompt"
        ]
      }
    }
  }
}