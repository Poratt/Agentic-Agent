# Database
DATABASE_URL=mysql://user:password@localhost:3306/agentic-admin

# JWT
JWT_SECRET=<change-me>

# LLM Bootstrap provider (per-provider defaults are in DB)
AI_PROVIDER=openrouter

# LLM OpenRouter
OPENROUTER_API_KEY=sk-or-v1-<key>

# LLM NVIDIA
NVIDIA_API_KEY=<key>

# LLM Ollama (local)
OLLAMA_BASE_URL=http://localhost:11434

# LLM Encryption (MANDATORY)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=56a6205c9c2e9c4161933856aa7d93638e6ca851ba3719c47bd07fa59265c8a5
